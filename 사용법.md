# 매일 사용하는 법

## 전자동 (샵마인 엑셀 받기부터 송장 반영까지)

바탕화면 **"송장자동화"** 아이콘을 더블클릭하고, 초록색
**[⚡ 전부 자동으로 처리]** 버튼을 누르면 됩니다.

- 누르기 전에 **최대 반영 건수**(기본 100건)를 확인하세요. 조회 성공이 그보다
  많으면 반영하지 않고 멈춥니다
- 확인 창이 한 번 뜹니다. 내용을 읽고 [확인]을 누르면 시작합니다

터미널을 쓰고 싶으면 아래 한 줄도 같습니다.

```
cd C:\Users\admin\workspace\kny\auto-invoice
.venv\Scripts\activate
python scripts\run_all.py
```

이게 하는 일:

1. 샵마인이 다른 탭을 보고 있으면 **배송중 탭으로 옮기고** (탭이 닫혀 있으면
   **주문관리 > 배송중** 메뉴로 다시 열고)
2. **쇼핑몰이 전부 연결돼 있는지 확인하고**, 끊긴 곳이 있으면
   **연결안된 쇼핑몰 연결재시도** 를 눌러 다시 연결하고
3. 샵마인 **배송중** 탭에서 **송장수정모드**를 켜고, 택배사가 **경동택배** /
   **직접** 인 주문만 검색해서 전부 체크하고
4. 엑셀 양식이 **송장 자동화** 인지 확인하고 (다르면 그것으로 바꿔서)
   체크한 주문만 엑셀로 받고
5. 공급사에서 송장번호를 조회해서 업로드용 CSV를 만들고
6. **발송정보일괄등록(수정용)** 으로 그 CSV를 올려 **일괄등록** 을 눌러
   `송장번호(수정용)` 컬럼을 채우고
7. `송장번호(수정용)` **컬럼 제목을 두 번 눌러** 채워진 주문을 위로 정렬한 뒤
   그 주문만 체크해서 **송장번호수정** 을 한 번 눌러 쇼핑몰까지 반영하고,
   결과 창에 **문구가 뜰 때까지 기다렸다가 오류를 읽고 창을 닫고**
8. 오류가 없으면 체크를 풀고 **송장수정모드를 끕니다**
   (오류가 있으면 확인만 누르고, 송장수정모드는 켜둔 채로 끝냅니다 - 화면에서
   무엇이 반영됐는지 바로 볼 수 있게)

반영한 게 **하나도 없으면**(송장이 하나도 조회되지 않았거나 중간에 멈췄으면)
3단계에서 걸어둔 체크를 풀고 송장수정모드도 꺼서 **실행 전 상태로 되돌립니다.**
샵마인을 열었을 때 영문 모를 체크가 남아 있지 않게 하기 위해서입니다.
(송장수정모드를 원래 켜두셨다면 켜진 채로 둡니다.)

1단계를 먼저 하는 이유: 3단계부터는 **지금 보고 있는 탭**을 조작합니다. 다른
탭을 열어둔 채로 시작하면 엉뚱한 화면을 건드리게 되므로, 탭부터 맞춥니다.

2단계를 먼저 하는 이유: 7단계 **송장번호수정** 은 샵마인이 실제로 쇼핑몰에
접속해서 송장을 등록하는 동작이라, 연결이 끊겨 있으면 그 쇼핑몰 주문만
실패합니다. 이미 다른 주문이 반영된 뒤라 다시 맞추기 번거롭습니다.

경동택배·직접은 "아직 진짜 송장이 없다"는 표시라서, 그것만 골라 고치는
것입니다. 조회에 실패한 주문은 `송장번호(수정용)` 칸이 비어 있으니 7단계에서
저절로 빠집니다.

### 직접 채워 넣으신 송장은 건드리지 않습니다

미지원 사이트 주문처럼 사장님이 직접 조회해서 `송장번호(수정용)` 칸에 적어두신
행이 있으면, 도구가 올린 행과 화면에서 구분이 안 됩니다(샵마인 검색은 원본
데이터만 찾아서 송장번호로는 그 행을 집어낼 수 없습니다).

그래서 7단계는 이렇게 합니다.

- 평소에는 **경동택배 / 직접** 으로 검색해 정렬하고 채워진 행을 체크합니다 (빠름)
- 그런데 **건수가 예상과 다르면** — 직접 채워두신 행이 섞였다는 뜻이라 —
  **도구가 올린 주문번호로 한 건씩 다시 검색해서 그 행만** 체크합니다
  (건당 3~4초 더 걸립니다)

즉 직접 채워두신 값은 **그대로 남습니다.** 반영하고 싶으시면 샵마인에서 그 행을
체크하고 [송장번호수정]을 직접 눌러주세요.

### 도중에 멈췄을 때: [↻ 멈춘 지점부터 다시 시작]

실행 중에 마우스를 움직이거나 다른 창을 앞으로 가져오면 안전을 위해 그
자리에서 멈춥니다. 그때 **[⚡ 전부 자동으로 처리]를 다시 누르지 마세요** -
제일 오래 걸리는 5단계 송장조회를 처음부터 다시 하게 됩니다.

대신 그 아래 **[↻ 멈춘 지점부터 다시 시작]** 버튼을 누르면 됩니다. 버튼 밑에
`멈춘 실행: 2026-08-31 10:20:11 실행 / 조회 끝낸 주문 37건 / ...` 처럼 무엇을
이어서 하는지 적혀 있습니다.

- 이미 받아둔 **주문목록 엑셀을 그대로** 쓰고 (3~4단계 건너뜀)
- **이미 조회한 주문은 다시 조회하지 않고** 남은 주문만 이어서 하고
- 그다음 6~8단계로 넘어갑니다

멈춘 실행이 없으면 버튼은 꺼져 있습니다. 끝까지 마치면 저절로 지워집니다.

터미널에서는 `python scripts\run_all.py --resume` 입니다.

### 실행 전 준비

- 샵마인만 켜두면 됩니다. **배송중 탭은 알아서 엽니다** (다른 탭을 보고
  있어도, 배송중 탭을 닫아뒀어도 됩니다)
- **실행 중에는 마우스·키보드를 건드리지 마세요.** 자동화가 마우스 커서를
  쓰기 때문에, 도중에 마우스를 움직이면 안전을 위해 그 자리에서 멈춥니다
  (잘못 눌리지는 않습니다)
- 공급사 로그인 창이 뜨면 그때만 로그인해주면 자동으로 이어집니다

### 처음엔 조금만

```
python scripts\run_all.py --limit 5 --max-apply 5
```

`--limit 5` 는 공급사 조회를 5건만 한다는 뜻입니다.

### 일괄등록까지만 하고 마지막은 직접 누르고 싶을 때

```
python scripts\run_all.py --stop-before-apply
```

`송장번호(수정용)` 컬럼까지만 채우고 멈춥니다. 화면에서 확인한 뒤 채워진 행을
체크하고 **[송장번호수정]** 을 직접 누르면 됩니다.

### 중간에 멈췄을 때

건수가 상한을 넘어 멈추면 CSV는 이미 만들어져 있습니다. 내용을 확인한 뒤
그 파일로 6~8단계만 이어서 돌릴 수 있습니다 (공급사 조회를 다시 하지 않습니다).

```
python scripts\run_all.py --csv "%USERPROFILE%\Desktop\송장업로드_20260827_204637.csv"
```

## 스스로 멈추는 경우들

억지로 진행하지 않고 멈추도록 만들었습니다. 아래 메시지가 뜨면 **아무것도
반영되지 않은 상태**이니, 원인을 확인하고 다시 실행하면 됩니다.

| 메시지 | 뜻 |
|---|---|
| `반영 대상이 N건으로 상한을 넘습니다` | 예상보다 건수가 많음. 확인 후 `--max-apply` 를 올려 재실행 |
| `필터가 '...'로 걸리지 않았습니다` | 검색이 화면에 반영되지 않음. 엉뚱한 주문이 선택되는 걸 막은 것 |
| `경동택배/직접 에 해당하는 주문이 하나도 없습니다` | 오늘 고칠 송장이 없음 |
| `송장이 채워진 행이 N건으로 CSV보다 많습니다` | 화면에서 고른 행이 예상보다 많음. 아무것도 반영하지 않음 |
| `목록이 계속 불안정합니다` | 샵마인이 화면을 다 못 그림. 잠시 뒤 다시 실행 |
| `커서가 목표에 도달하지 못함` | 실행 중 마우스를 움직였음 |
| `[배송중] 탭으로 옮기지 못했습니다` | 탭 전환이 안 됨. 샵마인에서 직접 배송중 탭을 연 뒤 다시 실행 |
| `버튼이 다른 창에 가려져 있습니다` | 샵마인 위에 다른 창이 있음 |
| `반영 대상 건수가 예상과 다릅니다` | 확인창 건수가 안 맞아 **[아니요]** 를 누른 것 |
| `재연결을 시도했는데도 연결되지 않은 쇼핑몰이 남아 있습니다` | 샵마인 **[쇼핑몰연결(Y)] > [쇼핑몰연결창]** 에서 직접 확인 (보통 그 쇼핑몰 비밀번호가 바뀐 경우) |

## 송장 파일만 만들고 업로드는 직접 하고 싶을 때

같은 창 아래쪽의 **[▶ 파일만 만들기]** 를 쓰면 됩니다. 샵마인 화면은
전혀 건드리지 않고, 골라둔 엑셀로 업로드용 CSV만 만들어줍니다.

1. 샵마인 → **배송중** → 고칠 주문을 체크 → **[엑셀파일생성]** 으로 받아둡니다
   - 이때 **엑셀 양식은 반드시 `송장 자동화`** 로 골라야 합니다. 이 창은
     지난번에 쓴 양식을 그대로 띄우기 때문에, 다른 양식이면 컬럼이 달라
     파일을 읽지 못합니다. (전자동은 이걸 알아서 맞춰줍니다)
2. 바탕화면 **"송장자동화"** 아이콘 더블클릭
3. 최근 저장된 엑셀이 자동 선택되어 있습니다 (다른 파일은 "다른 파일 선택...")
4. **[▶ 파일만 만들기]** 클릭
5. 완료되면 **[업로드용 파일 열기]** 로 확인
6. 그 파일을 샵마인 **발송정보일괄등록(수정용)** 으로 업로드

## 결과 확인

- 실행 결과는 화면에 `반영 성공 N건 / 실패 N건` 으로 요약됩니다
- 송장조회 결과는 바탕화면 **`송장조회결과_날짜시각.xlsx`** 로도 저장됩니다
  ([조회 결과 엑셀 열기] 버튼). 주문별로 성공/실패/미지원 사이트/취소·품절과
  송장번호, 샵마인 반영 여부가 한 장에 정리돼 있고, **손이 필요한 건이 위로**
  올라옵니다 (두 번째 [요약] 시트에 건수와 파일 경로)
- **송장이 안 나와서 넘긴 주문 중 주문한 지 2일 이상 지난 건**은 따로 모아서
  알려줍니다 (예: 주문상세 8월 26일 / 오늘 8월 28일 → 발송이 늦어지는 중).
  화면 요약에 `주문일이 2일 이상 지난 주문` 목록으로 나오고, 결과 엑셀에서는
  '주문일' 칸이 빨갛게 표시되면서 **[주문일지연] 시트**에 며칠 지났는지까지
  정리됩니다. 송장을 받은 성공 건, 취소/품절 건은 여기 들어가지 않습니다
  (기다린다고 나올 건이 아니라서요 - 취소/품절은 자기 목록이 따로 있습니다)
- **주문일이 2일 이상 지난 건에는 두 가지를 더 적어둡니다.** 공급사 화면에
  `출고예정 2026-09-02` / `도착예정 ...` 같은 문구가 있었으면 **[출고/도착예정]**
  칸에, 그리고 그 주문의 **[상품URL]** 을 같이 적습니다. 사이트를 다시 찾아
  들어가지 않고도 '기다리면 되는 건지'를 바로 볼 수 있게 하려는 것입니다
  (예정 문구가 없던 주문은 빈칸입니다)
- **샵마인 [송장번호수정] 결과 창에 오류가 떴으면** 그 문구를 결과 엑셀에
  같이 남깁니다. 오류 문구에서 주문번호를 찾을 수 있으면 그 주문이 **[반영오류]**
  로 표시되어 취소/품절 바로 다음에 올라오고, 문구 원문은 [요약] 시트에도
  그대로 들어갑니다
- **중간에 멈춰도** 송장조회까지 끝났으면 이 엑셀은 만들어집니다. 화면 요약에도
  `송장조회는 끝냈습니다: 성공 N / 실패 N / 스킵 N` 이 함께 나오니, 그 결과로
  이어서 처리하면 됩니다 (업로드용 CSV도 이미 만들어져 있습니다)
- 상세 기록은 `logs\run_*.json` 에 남습니다 (고객 개인정보는 기록하지 않습니다)
- 만들어진 엑셀/CSV는 **바탕화면**에 날짜시각 이름으로 쌓입니다

---

문제 생기면 화면에 나온 메시지와 함께 물어보면 됩니다. 자세한 배경 설명은
`README.md` 참고.
