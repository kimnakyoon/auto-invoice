# 매일 사용하는 법

## 전자동 (샵마인 엑셀 받기부터 송장 반영까지)

바탕화면 **"송장자동화"** 아이콘을 더블클릭하고, 초록색
**[⚡ 전부 자동으로 처리]** 버튼을 누르면 됩니다.

- 누르기 전에 **최대 반영 건수**(기본 30건)를 확인하세요. 조회 성공이 그보다
  많으면 반영하지 않고 멈춥니다
- 확인 창이 한 번 뜹니다. 내용을 읽고 [확인]을 누르면 시작합니다

터미널을 쓰고 싶으면 아래 한 줄도 같습니다.

```
cd C:\Users\admin\workspace\kny\auto-invoice
.venv\Scripts\activate
python scripts\run_all.py --max-apply 30
```

이게 하는 일:

1. 샵마인 **배송중** 탭에서 주문 목록 엑셀을 받고
2. 공급사에서 송장번호를 조회해서 업로드용 CSV를 만들고
3. **발송정보일괄등록(수정용)** 으로 그 CSV를 올리고
4. **일괄등록** 을 눌러 `송장번호(수정용)` 컬럼을 채우고
5. **송장번호수정** 으로 쇼핑몰까지 실제 반영합니다

### 실행 전 준비

- 샵마인을 켜고 **배송중 탭**을 열어두세요 (다른 탭이면 멈춥니다)
- **실행 중에는 마우스·키보드를 건드리지 마세요.** 자동화가 마우스 커서를
  쓰기 때문에, 도중에 마우스를 움직이면 안전을 위해 그 자리에서 멈춥니다
  (잘못 눌리지는 않습니다)
- 공급사 로그인 창이 뜨면 그때만 로그인해주면 자동으로 이어집니다

### 처음엔 조금만

```
python scripts\run_all.py --limit 5 --max-apply 5
```

`--limit 5` 는 공급사 조회를 5건만 한다는 뜻입니다.

### 4단계까지만 하고 마지막은 직접 누르고 싶을 때

```
python scripts\run_all.py --stop-before-apply
```

`송장번호(수정용)` 컬럼까지만 채우고 멈춥니다. 화면에서 확인한 뒤
**[송장번호수정]** 을 직접 누르면 됩니다.

### 중간에 멈췄을 때

건수가 상한을 넘어 멈추면 CSV는 이미 만들어져 있습니다. 내용을 확인한 뒤
그 파일로 3~5단계만 이어서 돌릴 수 있습니다 (공급사 조회를 다시 하지 않습니다).

```
python scripts\run_all.py --csv "work\송장업로드_20260827_204637.csv" --max-apply 30
```

## 스스로 멈추는 경우들

억지로 진행하지 않고 멈추도록 만들었습니다. 아래 메시지가 뜨면 **아무것도
반영되지 않은 상태**이니, 원인을 확인하고 다시 실행하면 됩니다.

| 메시지 | 뜻 |
|---|---|
| `반영 대상이 N건으로 상한을 넘습니다` | 예상보다 건수가 많음. 확인 후 `--max-apply` 를 올려 재실행 |
| `필터가 '...'로 걸리지 않았습니다` | 그 주문이 목록에 없음. 엉뚱한 주문에 반영되는 걸 막은 것 |
| `커서가 목표에 도달하지 못함` | 실행 중 마우스를 움직였음 |
| `버튼이 다른 창에 가려져 있습니다` | 샵마인 위에 다른 창이 있음 |
| `반영 대상 건수가 예상과 다릅니다` | 확인창 건수가 안 맞아 **[아니요]** 를 누른 것 |

## 송장 파일만 만들고 업로드는 직접 하고 싶을 때

같은 창 아래쪽의 **[▶ 파일만 만들기]** 를 쓰면 됩니다. 샵마인 화면은
전혀 건드리지 않고, 골라둔 엑셀로 업로드용 CSV만 만들어줍니다.

1. 샵마인 → **배송중** → **[엑셀파일생성]** 으로 엑셀을 받아둡니다
2. 바탕화면 **"송장자동화"** 아이콘 더블클릭
3. 최근 저장된 엑셀이 자동 선택되어 있습니다 (다른 파일은 "다른 파일 선택...")
4. **[▶ 파일만 만들기]** 클릭
5. 완료되면 **[결과 파일 열기]** 로 확인
6. 그 파일을 샵마인 **발송정보일괄등록(수정용)** 으로 업로드

## 결과 확인

- 실행 결과는 화면에 `반영 성공 N건 / 실패 N건` 으로 요약됩니다
- 상세 기록은 `logs\run_*.json` 에 남습니다 (고객 개인정보는 기록하지 않습니다)
- 만들어진 엑셀/CSV는 `work\` 폴더에 날짜시각 이름으로 쌓입니다

---

문제 생기면 화면에 나온 메시지와 함께 물어보면 됩니다. 자세한 배경 설명은
`README.md` 참고.
