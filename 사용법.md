# 매일 사용하는 법

## 1. 터미널 열기
시작 메뉴 → "PowerShell" 검색 → Windows PowerShell 실행

## 2. 프로젝트 폴더로 이동 + 가상환경 켜기
```
cd C:\Users\admin\workspace\kny\auto-invoice
.venv\Scripts\activate
```
왼쪽에 `(.venv)`가 보이면 성공.

## 3. 샵마인에서 발송대상 엑셀 내보내기
샵마인 → **주문관리 > 발송대상** → **[엑셀파일생성]** 클릭.
저장된 파일 경로를 기억해둔다 (보통 바탕화면).

## 4. 자동화 실행
```
python scripts\run.py --input "저장한 파일의 전체 경로.xls"
```
- 브라우저 로그인 요청 메시지가 뜨면 로그인 후 터미널에서 Enter
- 처음엔 `--limit 5`를 붙여서 소량 테스트 추천

## 5. 결과 확인
`완료: 성공 N / 실패 N / 스킵 N` 확인. 실패가 있으면 스크린샷으로 문의.

## 6. 생성된 파일 확인
바탕화면에 `송장자동화_오늘날짜.csv` 파일이 생성됩니다 (예: `송장자동화_20260826.csv`). 엑셀로 열어 내용 확인.

## 7. 샵마인에 업로드
샵마인 → **발송정보일괄등록(수정용)** → 찾아보기로 바탕화면의 `송장자동화_오늘날짜.csv` 선택 → **일괄등록**.

## 8. 끝
터미널 닫으면 됨.

---

문제 생기면 스크린샷과 함께 물어보면 됨. 자세한 배경 설명은 `README.md` 참고.
